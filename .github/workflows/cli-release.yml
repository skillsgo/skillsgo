# [INPUT]: Depends on cli/v* tags reachable from main, the unified CLI build contract, GitHub Releases, SBOM generation, and artifact attestations.
# [OUTPUT]: Publishes five standalone CLI archives, npm packages, a Homebrew Formula, SBOMs, checksums, an unsigned provider-neutral CDN Manifest payload, attestations, and one immutable GitHub Release.
# [POS]: Serves as the public verify-build-assemble control plane; an embedding publisher owns Manifest signing, immutable hosting, read-back, and stable-pointer publication.
# [PROTOCOL]: Update this header when this file changes, then review AGENTS.md
name: CLI Release

on:
  push:
    tags:
      - cli/v*
  workflow_dispatch:
    inputs:
      version:
        description: Candidate version, for example v0.1.0-rc.1
        required: true
        default: v0.0.0-dev

concurrency:
  group: cli-release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  verify:
    name: Verify CLI release
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    outputs:
      version: ${{ steps.version.outputs.version }}
      version_number: ${{ steps.version.outputs.version_number }}
      published_at: ${{ steps.version.outputs.published_at }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: '1.26.5'
          cache-dependency-path: cli/go.sum

      - name: Resolve and validate version
        id: version
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          REF_NAME: ${{ github.ref_name }}
          MANUAL_VERSION: ${{ inputs.version }}
        run: |
          if [[ "${EVENT_NAME}" == "push" ]]; then
            if [[ ! "${REF_NAME}" =~ ^cli/v ]]; then
              echo "Invalid CLI tag: ${REF_NAME}" >&2
              exit 1
            fi
            version="${REF_NAME#cli/}"
            git fetch origin main
            if ! git merge-base --is-ancestor "${GITHUB_SHA}" origin/main; then
              echo "CLI release commit must be reachable from main." >&2
              exit 1
            fi
          else
            version="${MANUAL_VERSION}"
          fi
          GOWORK=off go -C cli run ./cmd/skillsgo-release-manifest --validate-version "${version}"
          echo "version=${version}" >> "${GITHUB_OUTPUT}"
          echo "version_number=${version#v}" >> "${GITHUB_OUTPUT}"
          commit_timestamp="$(git show -s --format=%ct "${GITHUB_SHA}")"
          echo "published_at=$(date --utc --date="@${commit_timestamp}" +'%Y-%m-%dT%H:%M:%SZ')" >> "${GITHUB_OUTPUT}"

      - name: Test standalone module
        working-directory: cli
        run: |
          GOWORK=off go mod download
          GOWORK=off go test ./...
          GOWORK=off go build ./cmd/skillsgo

      - name: Test release build contract
        run: ./scripts/test-build-cli-release.sh

      - name: Verify public go install contract
        if: github.event_name == 'push'
        env:
          RELEASE_VERSION: ${{ steps.version.outputs.version }}
        run: |
          install_root="${RUNNER_TEMP}/go-install"
          GOWORK=off GOPROXY=direct GOBIN="${install_root}" \
            go install "github.com/skillsgo/skillsgo/cli/cmd/skillsgo@${RELEASE_VERSION}"
          handshake="$("${install_root}/skillsgo" version --output json)"
          if [[ "${handshake}" != *"\"version\":\"${RELEASE_VERSION}\""* || \
            "${handshake}" != *'"distribution":"go-install"'* ]]; then
            echo "go install produced an invalid CLI identity: ${handshake}" >&2
            exit 1
          fi

  build:
    name: Build CLI ${{ matrix.goos }}/${{ matrix.goarch }}
    needs: verify
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      contents: read
      id-token: write
      attestations: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: darwin
            goarch: arm64
          - goos: darwin
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: linux
            goarch: amd64
          - goos: windows
            goarch: amd64
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: '1.26.5'
          cache-dependency-path: cli/go.sum

      - name: Build release archive
        env:
          SKILLSGO_CLI_COMMIT: ${{ github.sha }}
          SKILLSGO_CLI_BUILD_DATE: ${{ needs.verify.outputs.published_at }}
        run: ./scripts/build-cli-release.sh ${{ needs.verify.outputs.version }} ${{ matrix.goos }} ${{ matrix.goarch }} "$RUNNER_TEMP/cli-release" direct

      - name: Generate binary SBOM
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0
        with:
          file: ${{ runner.temp }}/cli-release/skillsgo-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.goos == 'windows' && '.exe' || '' }}
          format: spdx-json
          output-file: ${{ runner.temp }}/cli-release/skillsgo_${{ needs.verify.outputs.version_number }}_${{ matrix.goos }}_${{ matrix.goarch }}.spdx.json
          upload-artifact: false
          upload-release-assets: false

      - name: Attest Unix release archive
        if: matrix.goos != 'windows'
        uses: actions/attest-build-provenance@43d14bc2b83dec42d39ecae14e916627a18bb661 # v3
        with:
          subject-path: ${{ runner.temp }}/cli-release/skillsgo_${{ needs.verify.outputs.version_number }}_${{ matrix.goos }}_${{ matrix.goarch }}.tar.gz

      - name: Attest Windows release archive
        if: matrix.goos == 'windows'
        uses: actions/attest-build-provenance@43d14bc2b83dec42d39ecae14e916627a18bb661 # v3
        with:
          subject-path: ${{ runner.temp }}/cli-release/skillsgo_${{ needs.verify.outputs.version_number }}_${{ matrix.goos }}_${{ matrix.goarch }}.zip

      - name: Attest generated SBOM
        uses: actions/attest-build-provenance@43d14bc2b83dec42d39ecae14e916627a18bb661 # v3
        with:
          subject-path: ${{ runner.temp }}/cli-release/skillsgo_${{ needs.verify.outputs.version_number }}_${{ matrix.goos }}_${{ matrix.goarch }}.spdx.json

      - name: Upload platform release
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: cli-release-${{ matrix.goos }}-${{ matrix.goarch }}
          path: |
            ${{ runner.temp }}/cli-release/*.tar.gz
            ${{ runner.temp }}/cli-release/*.zip
            ${{ runner.temp }}/cli-release/*.spdx.json
          if-no-files-found: error
          retention-days: 14

  assemble:
    name: Assemble CLI release
    if: github.event_name == 'push'
    needs: [verify, build]
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: write
      id-token: write
      attestations: write
    env:
      RELEASE_VERSION: ${{ needs.verify.outputs.version }}
      PUBLISHED_AT: ${{ needs.verify.outputs.published_at }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: '1.26.5'
          cache-dependency-path: cli/go.sum

      - name: Set up Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: '22'

      - name: Download platform releases
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          pattern: cli-release-*
          path: release-assets
          merge-multiple: true

      - name: Assemble provider-neutral release metadata
        shell: bash
        run: |
          mkdir -p release-metadata
          GOWORK=off go -C cli run ./cmd/skillsgo-release-manifest \
            --assets "${GITHUB_WORKSPACE}/release-assets" \
            --version "${RELEASE_VERSION}" \
            --commit "${GITHUB_SHA}" \
            --published-at "${PUBLISHED_AT}" \
            --manifest "${GITHUB_WORKSPACE}/release-metadata/manifest.json" \
            --checksums "${GITHUB_WORKSPACE}/release-metadata/checksums.txt"

      - name: Assemble npm packages and Homebrew Formula
        shell: bash
        run: |
          node cli/npm/assemble.mjs \
            --version "${RELEASE_VERSION}" \
            --artifacts "${GITHUB_WORKSPACE}/release-assets" \
            --output "${GITHUB_WORKSPACE}/npm-packages"
          node scripts/generate-homebrew-formula.mjs \
            --version "${RELEASE_VERSION}" \
            --artifacts "${GITHUB_WORKSPACE}/release-assets" \
            --output "${GITHUB_WORKSPACE}/homebrew/Formula/skillsgo.rb"
          cp npm-packages/*.tgz release-metadata/
          cp homebrew/Formula/skillsgo.rb release-metadata/skillsgo.rb
          rm -rf package-manager-outputs
          mkdir -p package-manager-outputs
          cp npm-packages/*.tgz package-manager-outputs/
          cp homebrew/Formula/skillsgo.rb package-manager-outputs/skillsgo.rb

      - name: Upload package-manager outputs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: cli-package-manager-outputs
          path: package-manager-outputs/*
          if-no-files-found: error
          retention-days: 14

      - name: Attest release metadata
        uses: actions/attest-build-provenance@43d14bc2b83dec42d39ecae14e916627a18bb661 # v3
        with:
          subject-path: |
            release-metadata/checksums.txt
            release-metadata/manifest.json

      - name: Attest package-manager outputs
        uses: actions/attest-build-provenance@43d14bc2b83dec42d39ecae14e916627a18bb661 # v3
        with:
          subject-path: |
            npm-packages/*.tgz
            homebrew/Formula/skillsgo.rb

      - name: Publish immutable GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        run: |
          release_flags=()
          if [[ "${RELEASE_VERSION}" == *-* ]]; then
            release_flags+=(--prerelease)
          fi
          gh release create "${TAG}" release-assets/* release-metadata/* "${release_flags[@]}" \
            --repo "${GITHUB_REPOSITORY}" \
            --verify-tag \
            --title "SkillsGo CLI ${RELEASE_VERSION}" \
            --generate-notes

  publish-npm:
    name: Publish npm packages
    if: github.event_name == 'push' && vars.NPM_PUBLISH_ENABLED == 'true'
    needs: [verify, assemble]
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
      id-token: write
    env:
      RELEASE_VERSION: ${{ needs.verify.outputs.version }}
    steps:
      - name: Set up Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: '24'
          registry-url: https://registry.npmjs.org
          package-manager-cache: false

      - name: Download package-manager outputs
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: cli-package-manager-outputs
          path: package-manager-outputs

      - name: Publish platform packages before the launcher
        shell: bash
        run: |
          set -euo pipefail
          main_package="${GITHUB_WORKSPACE}/package-manager-outputs/skillsgo-${RELEASE_VERSION#v}.tgz"
          for package in "${GITHUB_WORKSPACE}"/package-manager-outputs/skillsgo-*.tgz; do
            if [[ "${package}" != "${main_package}" ]]; then
              npm publish "${package}" --access public --provenance
            fi
          done
          npm publish "${main_package}" --access public --provenance

  publish-homebrew:
    name: Publish Homebrew Formula
    if: github.event_name == 'push' && vars.HOMEBREW_PUBLISH_ENABLED == 'true'
    needs: [verify, assemble]
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
    env:
      RELEASE_VERSION: ${{ needs.verify.outputs.version }}
      HOMEBREW_TAP_REPOSITORY: ${{ vars.HOMEBREW_TAP_REPOSITORY || 'skillsgo/homebrew-skillsgo' }}
      HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
    steps:
      - name: Download package-manager outputs
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: cli-package-manager-outputs
          path: package-manager-outputs

      - name: Publish Formula to the Homebrew tap
        shell: bash
        run: |
          set -euo pipefail
          git clone "https://x-access-token:${HOMEBREW_TAP_TOKEN}@github.com/${HOMEBREW_TAP_REPOSITORY}.git" tap
          mkdir -p tap/Formula
          cp package-manager-outputs/skillsgo.rb tap/Formula/skillsgo.rb
          git -C tap config user.name "github-actions[bot]"
          git -C tap config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git -C tap add Formula/skillsgo.rb
          if git -C tap diff --cached --quiet; then
            echo "Formula is already current."
            exit 0
          fi
          git -C tap commit -m "skillsgo ${RELEASE_VERSION}"
          git -C tap push origin HEAD
